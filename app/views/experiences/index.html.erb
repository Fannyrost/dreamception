<%= render 'shared/navbar_home' %>

<div class="banner">
  <div class="banner-content">
    <h1>Dreamception</h1>
    <p>Enter the void</p>
  </div>
</div>


<div class="container mt-5">
  <h2>Découvrez nos séjours en immersion</h2>

    <h3 class="mt-5 mb-2">Sciences Fiction</h3>
</div>
    <div class="scrolling-wrapper">
      <% @experiences.each do |experience| %>
        <% if experience.category == "sciences fiction" %>
        <%= link_to experience_path(experience) do %>
          <div class="card-homepage d-flex align-items-end" style= "background-image: url('<%= experience.photo_url %>')">
            <div class="card-hp-content px-5 py-3 d-flex justify-content-between">
              <div>
                <h4> <%= experience.title %> </h4>
                <p><%= experience.short_description %></p>
              </div>
            <div class="d-flex">
              <% if experience.average_rating != nil %>
                <% n = experience.average_rating.truncate() %>
                <% n.times do %>
                  <div><p><span  class="stars"><i class="fas fa-star"></i></span></p></div>
                <% end %>
                <% (5-n).times do %>
                  <div><p><span  class="stars"><i class="far fa-star"></i></span></p></div>
                <% end %>
              <% end %>
            </div>
          </div>
        </div>

     <% end %>
    <% end %>
  <% end %>
        </div>

        <div class="container mt-5">
            <h3 class="mt-5 mb-2">Histoire</h3>
        </div>
    <% @experiences.each do |experience| %>
      <% if experience.category == "histoire" %>
      <%= link_to experience_path(experience) do %>
        <div class="card-homepage d-flex align-items-end" style= "background-image: url('<%= experience.photo_url %>')">
          <div class="card-hp-content px-5 py-3 d-flex justify-content-between">
            <div>
              <h4> <%= experience.title %> </h4>
              <p><%= experience.short_description %></p>
            </div>
          <div class="d-flex">
            <% if experience.average_rating != nil %>
              <% n = experience.average_rating.truncate() %>
              <% n.times do %>
                <div><p><span  class="stars"><i class="fas fa-star"> </i> </span></p></div>
              <% end %>
              <% (5-n).times do %>
                <div><p><span  class="stars"><i class="far fa-star"></i></span> </p></div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
     <% end %>
    <% end %>
  <% end %>

  <div class="container mt-5">
      <h3 class="mt-5 mb-2">Sensations</h3>
  </div>
    <% @experiences.each do |experience| %>
      <% if experience.category == "sensations" %>
      <%= link_to experience_path(experience) do %>
        <div class="card-homepage d-flex align-items-end" style= "background-image: url('<%= experience.photo_url %>')">
          <div class="card-hp-content px-5 py-3 d-flex justify-content-between">
            <div>
              <h4> <%= experience.title %> </h4>
              <p><%= experience.short_description %></p>
            </div>
          <div class="d-flex">
            <% if experience.average_rating != nil %>
              <% n = experience.average_rating.truncate() %>
              <% n.times do %>
                <div><p><span  class="stars"><i class="fas fa-star"></i></span> </p></div>
              <% end %>
              <% (5-n).times do %>
                <div><p><span  class="stars"><i class="far fa-star"></i> </span></p></div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
     <% end %>
    <% end %>
  <% end %>

  <div class="container mt-5">
      <h3 class="mt-5 mb-2">Insolite</h3>
  </div>
    <% @experiences.each do |experience| %>
      <% if experience.category == "insolite" %>
      <%= link_to experience_path(experience) do %>
        <div class="card-homepage d-flex align-items-end" style= "background-image: url('<%= experience.photo_url %>')">
          <div class="card-hp-content px-5 py-3 d-flex justify-content-between">
            <div>
              <h4> <%= experience.title %> </h4>
              <p><%= experience.short_description %></p>
            </div>
          <div class="d-flex">
            <% if experience.average_rating != nil %>
              <% n = experience.average_rating.truncate() %>
              <% n.times do %>
                <div><p><span  class="stars"><i class="fas fa-star"></i></span> </p></div>
              <% end %>
              <% (5-n).times do %>
                <div><p><span  class="stars"><i class="far fa-star"></i></span> </p></div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
     <% end %>
    <% end %>
  <% end %>


